{
  "name": "@release-bot/db",
  "version": "1.0.0",
  "description": "Share database logic between packages",
  "main": "src/index.ts",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "check": "biome check",
    "prebuild": "prisma generate",
    "build": "tsc -p tsconfig.build.json",
    "test": "vitest",
    "test:run": "vitest --run",
    "clean": "rm -rf dist",
    "dev:db:generate": "prisma generate",
    "dev:db:migrate": "prisma migrate dev"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.33.0",
  "devDependencies": {
    "@types/node": "catalog:",
    "dotenv": "catalog:",
    "prisma": "^7.8.0",
    "tsx": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  },
  "dependencies": {
    "@prisma/adapter-pg": "catalog:",
    "@prisma/client": "^7.8.0",
    "pg": "^8.20.0"
  }
}
